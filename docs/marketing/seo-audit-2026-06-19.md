# remem SEO and Discoverability Audit

> Audit date: 2026-06-19
> Target: GitHub repository, GitHub Pages site, npm package, crates.io/lib.rs surface, and search-result discoverability for `majiayu000/remem`.
> Goal: Keep SEO work separate from code-quality issues. This document is about being findable as a product.

## Baseline

| Surface | Current evidence | Status |
|---|---|---|
| GitHub repo | `majiayu000/remem`, public, description is keyword-rich, homepage points to GitHub Pages, topics include `claude-code`, `codex-cli`, `agent-memory`, `ai-memory`, `mcp-server`, `model-context-protocol`, `persistent-memory` | Healthy |
| GitHub Pages | `https://majiayu000.github.io/remem/` returns HTTP 200; homepage has title, meta description, canonical, OG/Twitter metadata, JSON-LD, and Search Console verification file | Healthy base |
| Robots/sitemap | `robots.txt` allows crawl and points to sitemap; live sitemap lists homepage plus the four long-tail pages added by PR #539 | Healthy; needs Search Console follow-through |
| npm | `@remem-ai/remem@0.5.97` is live with homepage, repository, bugs URL, keywords, and public publish config | Healthy base |
| crates.io | `remem-ai = 0.5.97` is discoverable through `cargo search`; Cargo metadata has description, repository, readme, keywords, categories | Healthy base; homepage still points to GitHub repo, not product site |
| Search results | Exact product/repo queries find GitHub and GitHub Pages; broad `Remem AI` queries also surface an unrelated iOS/Product Hunt product | Needs brand disambiguation |

Commands and checks used:

```bash
git fetch --prune origin
gh repo view --json nameWithOwner,description,homepageUrl,repositoryTopics,visibility,defaultBranchRef,url
gh issue view 538 --json number,title,body,labels,url,state,comments
npm view @remem-ai/remem --json
cargo search remem --limit 5
curl -fsSI https://majiayu000.github.io/remem/
curl -fsS https://majiayu000.github.io/remem/robots.txt
curl -fsS https://majiayu000.github.io/remem/sitemap.xml
```

Search checks on 2026-06-19 also found:

- GitHub repo result for `majiayu000/remem`.
- GitHub Pages homepage result for `remem - Persistent memory for Claude Code and OpenAI Codex`.
- npm exact package references for `@remem-ai/remem`.
- Unrelated "Remem AI" Product Hunt/iOS results competing on the broad brand phrase.

## Existing SEO Issue

Issue #538 covered the main long-tail SEO work and was closed by PR #539:

- README first-screen positioning and conversion.
- Long-tail pages:
  - `/claude-code-memory/`
  - `/codex-memory/`
  - `/mcp-memory-server/`
  - `/compare/built-in-memory/`
- Sitemap updates.
- Package metadata tuning where scoped and safe.

Do not duplicate #538. Treat the long-tail content creation work as complete on current `main`.

Baseline comment added: https://github.com/majiayu000/remem/issues/538#issuecomment-4751005142
Correction comment after deploy verification: https://github.com/majiayu000/remem/issues/538#issuecomment-4751018793

## Findings

### S1 - Long-tail pages are live

**Fact:** PR #539 added and deployed:

- `https://majiayu000.github.io/remem/claude-code-memory/`
- `https://majiayu000.github.io/remem/codex-memory/`
- `https://majiayu000.github.io/remem/mcp-memory-server/`
- `https://majiayu000.github.io/remem/compare/built-in-memory/`

The live sitemap lists all four URLs, and each URL returned HTTP 200 on 2026-06-19.

**Closed issue:** #538 - https://github.com/majiayu000/remem/issues/538.

**Merged PR:** #539 - https://github.com/majiayu000/remem/pull/539.

**Remaining work:** Search Console submission/request-indexing is tracked in #554.

### S2 - Broad brand phrase collides with an unrelated "Remem AI" product

**Fact:** Search results for broad `Remem AI` terms include an unrelated iOS/Product Hunt product. This repo already uses stronger phrases like "Persistent memory for Claude Code and OpenAI Codex", but the site and registries should keep reinforcing the developer-tool identity.

**Impact:** Users searching from memory may land on the wrong "Remem AI" unless snippets include `Claude Code`, `Codex`, `MCP`, `Rust CLI`, and `coding agent memory`.

**Done when:** Homepage/schema/package copy consistently uses disambiguating phrases; JSON-LD `sameAs` includes GitHub, npm, crates.io, docs.rs/lib.rs; README first screen and package descriptions avoid generic "AI memory" alone.

**Owner issue:** #552 - https://github.com/majiayu000/remem/issues/552.

### S3 - Cargo homepage differs from npm and site canonical

**Fact:** npm homepage points to `https://majiayu000.github.io/remem/`, while `Cargo.toml` homepage points to the GitHub repository.

**Impact:** crates.io/lib.rs users and search crawlers get a weaker product-site signal than npm users.

**Done when:** Cargo homepage points to the product site, repository remains the GitHub URL, and `cargo publish --dry-run --locked` is verified before release.

**Owner issue:** #553 - https://github.com/majiayu000/remem/issues/553.

### S4 - Search Console follow-through is now the main indexing task

**Fact:** The site has a Google verification file and crawlable sitemap. The long-tail pages are live, but Google indexing still depends on Search Console submission/request-indexing and crawler timing.

**Impact:** The repository can ship SEO pages while search indexing lags or is never manually requested.

**Done when:** Submit the updated sitemap in Search Console, request indexing for the homepage and four long-tail URLs, and record the submission date/status.

**Owner issue:** #554 - https://github.com/majiayu000/remem/issues/554.

### S5 - Registry/package smoke should be part of SEO release proof

**Fact:** npm and crates surfaces are discoverability surfaces. The package is currently live, but future metadata changes should prove the install and version path still works after publishing.

**Impact:** SEO metadata can look correct while the package install path is broken, hurting conversion from search traffic.

**Related engineering issue:** #550 - https://github.com/majiayu000/remem/issues/550.

**Done when:** Any package metadata release verifies `npm pack`/install/`remem --version`; crates changes verify `cargo publish --dry-run --locked`.

## Recommended SEO Issue Plan

1. Treat #538/#539 as completed long-tail page work.
2. Use #552 for brand disambiguation across homepage, README, JSON-LD, npm, and Cargo.
3. Use #553 for Cargo homepage/site metadata alignment.
4. Use #554 for Search Console/indexing follow-through now that #539 is live.
5. Use #550 for package install smoke as SEO conversion proof.

## Non-SEO Findings

The separate code-quality/security audit lives in `docs/audit/audit-2026-06-19.md`.
Those issues are important but are not SEO tasks. They should be tracked as
engineering backlog so SEO work can continue without mixing concerns.
